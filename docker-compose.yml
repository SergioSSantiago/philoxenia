services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: philoxenia
      POSTGRES_PASSWORD: philoxenia
      POSTGRES_DB: philoxenia
    ports:
      - "5432:5432"
    volumes:
      - philoxenia_pg:/var/lib/postgresql/data

volumes:
  philoxenia_pg:
