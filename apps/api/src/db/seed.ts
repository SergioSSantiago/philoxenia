async function seed() {
  console.log("Philoxenia seed: users are created via wallet authentication.");
  console.log("Run the acceptance scenario manually after starting dev servers.");
}

seed().catch(console.error);
